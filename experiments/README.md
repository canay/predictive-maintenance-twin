# F08 Experiment Registry

Kanonik run klasörü standardı: `<YYYY-MM-DD>_<tool>_<location>_<run_slug>`.

✅ Aktif kanıtlar manuscript-facing veya release-facing evidence olarak kullanılır.  
⚠️ Auxiliary/legacy kayıtlar claim genişletmek için değil, sınır ve provenance korumak için okunur.  
❗ G11 hâlâ public GitHub repository/link-test ve final RESS package QA ile sınırlıdır; registry temizliği G11'i tek başına passed yapmaz.

Eski artifact-level tablo `MD/05_experiments/ARTIFACT_TO_TABLE_REGISTRY.csv` içinde korunmuştur.
